# Database connection
DATABASE_URL=postgresql://user:password@host:5432/database

# Path on NAS where attachments are stored (mounted into container as /data/attachments)
HOST_ATTACHMENT_PATH=/volume1/email_attachments

# Optional settings
POLL_INTERVAL=5
BATCH_SIZE=10
MAX_RETRIES=3
LOG_LEVEL=INFO

# Betterstack logging (optional)
BETTERSTACK_SOURCE_TOKEN=

